language: python
python:
  - "2.7"
cache: pip
install:
  - pip install -r requirements.txt
script:
  - python tests.pylanguage: python
python:
  - "2.6"
  - "2.7"
  - "3.2"

# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script: pytest
